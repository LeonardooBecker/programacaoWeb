<div id="opestado<%= elem.aluno_id %>" style="display: none" class="allModal">
    <div class="textModal">
        <span>
            Aluno:
            <%= elem.aluno.nome %>
        </span>
        <span>
            Departamento:
            <%= elem.departamento.nome %>
        </span>
        
        <% if current_user.role == 'admin' %>
        <div class=operaModal>
            <span>
                <%= button_to "Destroy this AlunoDepartamento", delete_aluno_departamento_path(elem.aluno), method: :delete %>
            </span>
            </div>
        <% end %>
        
        
    

    </div>
</div>